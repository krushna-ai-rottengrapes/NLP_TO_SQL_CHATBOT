# NLP to SQL

A natural language processing application that converts natural language queries to SQL statements.

## Project Structure

- `backend/` - Python backend with NLP processing
- `frontend/` - Next.js frontend application

## Getting Started

### Backend
```bash
cd backend
pip install -r requirements.txt
python main.py
```

### Frontend
```bash
cd frontend
npm install
npm run dev
```